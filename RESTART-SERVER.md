# Restart Your Dev Server

The API route has been updated with better error handling.

**Please do this:**

1. Stop your current dev server (Ctrl+C in the terminal)
2. Run: `npm run dev`
3. Try submitting a donation again
4. Check the terminal for detailed error messages

The logs will now show exactly what's failing!
